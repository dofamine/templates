import { TemplateEditableDirective } from './template-editable.directive';

describe('TemplateEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateEditableDirective();
    expect(directive).toBeTruthy();
  });
});
